#include <stdio.h>
#include <stdlib.h>
#include <math.h>


int main()
{
    unsigned int N, X, graos = 1, i, soma = 1, kg;

    scanf("%d", &N);

        for (i = 0; i < N; i++){
            scanf("%d", &X);
            printf("%d kg\n", (long long)(pow(2,kg)/12000));

        }
    return 0;
}
